#include <stdio.h>

/**
 * _strcpy - function
 * @src: source
 * @dest: destination
 * Return: str
 */

char *_strcpy(char *dest, char *src)
{
	/* int cnt = 0;
	char *tmp = src; */

	dest = src;
	/* while (*tmp)
	{
		cnt++;
		tmp++;
	} */
	return (dest);
}
